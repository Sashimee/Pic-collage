You are implementing the remaining features for the Pic-Collage project at /home/alex/projects/Pic-collage.

CRITICAL RULES — NO EXCEPTIONS:
1. Work on branch `dev`. Commit after every major task with clear messages.
2. Run `npm run lint` and `npx vitest run` after every change. Fix errors immediately.
3. Add DE+EN translations for ALL new UI strings in src/i18n/translations.ts.
4. NO questions to the user. Make decisions and execute.
5. If a task is blocked, skip it and move to the next one.

CURRENT STATE (already implemented):
- Undo/redo with snapshot-based history
- Layer panel (z-order, visibility, lock)
- Drawing tool
- Auto-save to IndexedDB via cloudSync.ts
- Photo shapes (rect, circle, star, heart)
- Templates gallery
- Background gradients + patterns + photo background
- Grid mode with gap/radius controls
- Frame styles (none, solid, rounded, polaroid)

WHAT IS MISSING — IMPLEMENT IN THIS ORDER:

---

TASK 1: Multi-Project Save/Load System

Create src/store/projectsStore.ts:
- Zustand store with: projects list (id, name, createdAt, updatedAt), activeProjectId
- Actions: createProject(name), openProject(id), renameProject(id, name), duplicateProject(id), deleteProject(id), listProjects()
- Save project data via cloudSync.ts saveProject / loadProject / deleteProject / listProjects
- On createProject: capture current editor state as the project data

Create src/components/ProjectManager.tsx:
- Bottom sheet / modal UI listing all saved projects
- Each project shows name, date, thumbnail placeholder
- Actions: Open, Rename, Duplicate, Delete
- "New Project" button at top
- Close button

Modify src/components/HeaderBar.tsx:
- Replace the "New" button with a dropdown/menu: New / Open / Save / Export
- "New" → clearAll() after confirming if unsaved changes exist
- "Open" → opens ProjectManager
- "Save" → saves current state to active project (or prompts for name if no active project)

Modify src/store/editorStore.ts:
- The auto-save subscription at the bottom should save to activeProjectId if set, otherwise skip (the 'auto' project is redundant once we have named projects)

Write tests in src/store/projectsStore.test.ts.

---

TASK 2: Portable .piccollage File Format

Create src/lib/projectFile.ts:
- export function packProject(project: Project): Blob
  - Build a JSON object with project metadata + elements (replace blob URLs with photoId references)
  - Look up each photoId in IndexedDB (getPhoto), base64-encode the blob
  - Include background photo if present
  - Return as Blob with MIME application/json
- export async function unpackProject(blob: Blob): Promise<{ doc: LoadedDocument; photos: { id: string; blob: Blob }[] }>
  - Parse JSON
  - Decode base64 photos back to Blobs
  - Return document + photos to be stored via putPhoto

Wire into HeaderBar:
- "Save as .piccollage" downloads the packed file
- "Open .piccollage" uses a hidden file input, unpacks, loads document, stores photos, calls loadDocument()

Write tests in src/lib/projectFile.test.ts.

---

TASK 3: Plugin Registry

Create src/lib/plugins/types.ts:
- FilterPlugin interface: id, name, nameDe, apply(filters) => patch
- StickerPackPlugin interface: id, name, nameDe, emojis

Create src/lib/plugins/registry.ts:
- Maps for filters and sticker packs
- registerFilter, registerStickerPack, getRegisteredFilters, getRegisteredStickerPacks

Modify src/lib/filters.ts:
- At module load, register all FILTER_PRESETS through the registry

Modify src/components/Panels.tsx:
- Read sticker list from registry instead of hardcoded
- Read filter presets from registry

Write tests in src/lib/plugins/registry.test.ts.

---

TASK 4: Layer Opacity + Blend Modes

Modify src/types.ts:
- Add opacity?: number (0-1, default 1) to BaseElement
- Add blendMode?: 'normal' | 'multiply' | 'screen' | 'overlay' to BaseElement

Modify src/components/CanvasNodes.tsx:
- Apply opacity to all node types
- Apply globalCompositeOperation for blend modes (Konva supports this via the shape)

Modify src/components/Panels.tsx or add to SelectionBar:
- Opacity slider (0-100%)
- Blend mode dropdown

Write tests verifying opacity/blend are rendered correctly.

---

TASK 5: Enhanced Export

Modify src/lib/exportImage.ts:
- Add format option: 'png' | 'jpeg' | 'webp'
- Add quality option (0-1) for lossy formats
- Add platform presets: 'instagram-square' (1080x1080), 'instagram-portrait' (1080x1350), 'instagram-story' (1080x1920), 'a4' (2480x3508)
- exportBoard(format, quality, preset) should resize/crop output accordingly

Modify src/components/HeaderBar.tsx:
- Export dropdown: Format (PNG/JPEG/WebP) + Platform preset
- Web Share API integration for mobile

---

TASK 6: Performance — Code Splitting + Lazy Loading

Modify vite.config.ts:
- Add manualChunks: vendor (react, react-dom, framer-motion), editor (konva, react-konva), ui (lucide-react, components)
- Set chunkSizeWarningLimit: 600

Modify src/App.tsx:
- React.lazy() import for EditorCanvas and Panels
- Suspense with simple spinner

---

TASK 7: More Tests

Add to src/store/editorStore.test.ts:
- Undo/redo round-trip for all action types
- loadDocument round-trip
- clearAll revokes blob URLs

Create src/lib/exportImage.test.ts:
- exportBoard returns valid Blob

---

TASK 8: CI/CD + Feature Flags

Modify .github/workflows/deploy.yml:
- Add workflow_dispatch trigger
- Add preview job (build on PRs, don't deploy)
- Cache node_modules

Create src/lib/features.ts:
- Feature flag system: isEnabled(flag) checks URL params then localStorage
- Use for drawing tool and template gallery (mark as experimental)

---

TASK 9: ADR + Docs

Create docs/adr/0001-client-only-architecture.md
Create docs/adr/0002-konva-stays.md
Create docs/user-guide.md (DE + EN)
Create docs/api.md (plugin API, store API)

---

TASK 10: Final Verification
- npm run lint → pass
- npx vitest run → all green
- npm run build → success
- Commit everything

GO. Start with Task 1 NOW. Do not ask questions. Execute.
