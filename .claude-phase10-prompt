You are implementing Phase 10 of the Pic-Collage app. Work on the `main` branch in `/home/alex/projects/Pic-collage`. Commit and push after each major task.

## CONTEXT
- Tech stack: React 18, TypeScript, Vite, Zustand, Konva (react-konva), Tailwind CSS, framer-motion, lucide-react
- Build: `npm run build` must succeed, `npm run lint` must pass (TypeScript strict)
- Tests: `npx vitest run` must pass 16+ tests
- i18n: Every UI string needs EN+DE entries in `src/i18n/translations.ts`
- IndexedDB: All access guarded with `typeof indexedDB === 'undefined'` for Node/test safety
- PWA: GitHub Pages deployment via `.github/workflows/deploy.yml`

## ALREADY BUILT (do not break)
- `src/store/editorStore.ts` — Zustand store with elements, undo/redo, shapes (rect/circle/star/heart), filters, grouping
- `src/components/CanvasNodes.tsx` — renders photo/text/sticker/drawing with clipFunc for shapes
- `src/components/Panels.tsx` — Photos/Layout/Text/Draw/Stickers/Background/Filters panels
- `src/components/LayoutPanel.tsx` — Grid layouts, board sizes, gap/radius
- `src/components/SelectionBar.tsx` — Opacity/blend controls for selected element
- `src/hooks/useShortcuts.ts` — Keyboard shortcuts (undo, redo, delete, select-all, save, export, open, escape)
- `src/components/ErrorBoundary.tsx` — Crash recovery UI
- `src/lib/importPhotos.ts` — `loadPhotoMeta()` with base64 fallback
- `src/lib/importFiles.ts` — `importFiles()` with async error handling
- `src/i18n/translations.ts` — EN/DE translations

## TASKS

### Task 1: Add 5 new shape mask functions
In `src/lib/shapes.ts` (create if not exists), add clip-path drawing functions for:
- `arch` — top half of an ellipse (like a rainbow/arch doorway)
- `diamond` — rotated square (45°)
- `cloud` — 3 overlapping circles forming a cloud silhouette
- `hexagon` — 6-sided polygon
- `triangle` — equilateral triangle pointing up
Each function takes `(ctx: CanvasRenderingContext2D, width: number, height: number)` and draws the shape path. Export them all.

### Task 2: Update CanvasNodes to support all shapes
In `src/components/CanvasNodes.tsx`, the `Pe` function already draws shapes (rect, circle, star, heart). Add cases for the 5 new shapes, calling the new functions from `src/lib/shapes.ts`. Update `Re` (shape options array) to include the new shapes with glyphs. The `clipFunc` on the Group already delegates to `Pe` — this should work automatically once `Pe` supports the new shapes.

### Task 3: ShapePicker in Layout Panel
In `src/components/LayoutPanel.tsx` (the component exported from Panels.tsx that shows grids/board-size), add a new section:
- Title: "Photo Shape" (i18n key: `shape.title`)
- Only visible when a photo element is selected
- Row of shape buttons (rect, circle, star, heart, arch, diamond, cloud, hexagon, triangle)
- Each button shows the glyph + label
- Clicking applies `updateElement(selectedId, { shape })`
- Active shape highlighted
- Add EN+DE translations for all new shape keys

### Task 4: Apply Shape to All Photos
In `src/store/editorStore.ts`, add `applyShapeToAll(shape: string)` action that updates the `shape` field on all elements where `type === 'photo'`. Add a button in the Layout panel: "Apply to all photos" (i18n: `shape.applyToAll`). Only visible when at least one photo exists.

### Task 5: Copy / Paste Elements
In `src/hooks/useShortcuts.ts`, add:
- `Ctrl+C` / `Cmd+C` — copy selected element JSON to clipboard (use navigator.clipboard.writeText with JSON.stringify, fallback to document.execCommand)
- `Ctrl+V` / `Cmd+V` — paste: parse JSON, create new element with `id: randomUUID()`, offset x/y by +20px, call `addPhoto`/`addText`/`addSticker`/`addDrawing` as appropriate
- Add i18n toast or console log on success/fail

### Task 6: Arrow Key Nudging
In `src/hooks/useShortcuts.ts`, add:
- Arrow keys → move selected element by 1px (updateElement with x/y)
- Shift+Arrow → move by 10px
- Prevent default to stop page scroll

### Task 7: Canvas Zoom & Pan Controls
Create `src/components/ZoomControls.tsx`:
- Show current zoom % (e.g. "100%")
- `-` / `+` buttons to zoom out/in (step 10%, range 25%–300%)
- "Reset" button to return to 100%
- "Fit" button to fit board to viewport
- Mount in `src/App.tsx` positioned absolute bottom-right of canvas area
- Store zoom in `editorStore` as `canvasZoom: number` (default 1)
- Apply zoom to the Konva Stage via `scaleX`/`scaleY` and adjust stage dimensions
- Pan: when zoom > 1, allow dragging on empty canvas to pan (use `draggable` on a background rect or stage)

### Task 8: Visual Polish
a) **Tooltips:** Add `title` attributes to all icon buttons in `HeaderBar.tsx`, `Panels.tsx`, `SelectionBar.tsx`
b) **Animated selection:** In `CanvasNodes.tsx`, when element is selected, draw an animated dashed border around it using a Konva `Rect` with `dash` array and an `offset` animation via `useFrame` or `setInterval`
c) **Confetti on export:** When export succeeds, fire a quick confetti burst. Install `canvas-confetti` (lightweight), create `src/lib/confetti.ts` that wraps it, call from `App.tsx` after successful export.
d) **Smooth photo add:** When a photo is added, animate its `opacity` from 0→1 and `scale` from 0.8→1 using framer-motion or Konva tweening. In `CanvasNodes.tsx`, add a `useEffect` on mount that tweens opacity for 300ms.

### Task 9: Smart Auto-Arrange on Drop
In `src/lib/importFiles.ts`, after `addPhoto` is called, if the store is in `grid` mode and there are empty grid cells, call `updateElement` to snap the photo into the next empty cell's position and size. This requires importing the store or passing a callback. Simpler approach: in `editorStore.ts`, in the `addPhoto` action, check if `mode === 'grid'` and `gridId` is set. If so, find the next empty cell in the current grid layout (`we()` helper exists), compute the cell's x/y/w/h from the board dimensions, and position the new photo there with `width: cell.w, height: cell.h`. This makes grid mode truly useful.

### Task 10: Build, Test, Deploy
1. Run `npm run lint` — fix all errors
2. Run `npm run build` — must succeed
3. Run `npx vitest run` — must pass
4. Commit with `feat(phase10): photo shape masks, copy/paste, zoom, nudge, confetti, smart grid`
5. Push to `origin/main`
6. Wait for GitHub Actions deploy
7. Verify production HTML serves new bundle

## IMPORTANT RULES
- Do NOT use `sudo`
- Do NOT ask the user questions
- If a build/test fails, fix it immediately
- If you hit a dead end, pivot to an alternative approach
- All UI strings must have EN+DE translations
- IndexedDB access must be guarded
